#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strcat - It adds a string to another string
 * @dest: The string thaat we will another string to
 * @src: The string to be added to another string
 * Return: Awlays 0
 */
char *_strcat(char *dest, char *src)
{
strcat(dest, src);
return (dest);
}
