#include "main.h"
/**
 * _isupper - It checks if a character is upper case
 * @c: character to be checked
 * Return: Always 0
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
else
{
return (0);
}
}
