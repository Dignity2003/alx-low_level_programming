#include <stdio.h>
/**
 * main - Entry point
 * @argc: It counts arguments
 * @argv: It holds strings
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
(void) argc;
printf("%s\n", argv[0]);

return (0);
}
