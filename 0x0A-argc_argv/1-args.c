#include <stdio.h>
/**
 * main - Starting point
 * @argc: argument count
 * @argv: Stores strings
 * Return: Always 0
 */
int main(int argc, char *argv)
{
(void) argv;
printf("%d\n", argc - 1);
return (0);
}
