#include "main.h"

/**
 * malloc_checked - Allocates Memory
 * @b: Size of memory
 * Return: Always 0
 */
void *malloc_checked(unsigned int b)
{
void *str;
str = malloc(str);
if (str == '\0')
{
exit(98);
}
return (str);
}
